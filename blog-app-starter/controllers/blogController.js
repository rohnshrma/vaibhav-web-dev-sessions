export const GET_HOME = (req, res) => {
  res.render("home");
};

export const GET_BLOGS = (req, res) => {
  res.render("blogs");
};
export const GET_COMPOSE = (req, res) => {
  res.render("compose");
};
